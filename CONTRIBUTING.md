# このリポジトリの編集ルール

マニュアル専用リポジトリです。本体コードは
[VRC_MirrorBallLight](https://github.com/BossNovice/VRC_MirrorBallLight) にあります。
両リポジトリは独立してコミット・Pushし、変更を混在させません。

## Gitブランチ運用

本体リポジトリと同じ規則です。常設ブランチは `main` だけで、作業ごとに短命なブランチを作ります。

```
work/YYYYMMDD-NN     例: work/20260821-01
hotfix/YYYYMMDD-NN   緊急修正のみ
```

**ブランチ名にバージョン番号や作業内容を含めません。** 何の作業かはPRのタイトルと本文で表現します。

この規則は過去の失敗を踏まえたものです。`agent/r25.1-surface-emission-audiolink` という名前の
ブランチがR27.2まで進み、名前と実態が乖離したまま `main` がR25時点で止まり続けました。
ブランチ名に内容を書くと、作業が続いた時点で必ず嘘になります。

### 運用ルール

- 1ブランチ1目的
- 作業開始直後にDraft PRを作る
- 原則3日以内、遅くとも7日以内にマージまたは破棄する
- マージ後はブランチを削除する（自動削除を有効化済み）
- `main` へ直接pushせず、必ずPRを経由する

### mainの保護

このリポジトリはpublicのため、GitHubのruleset（`main protection`）で保護しています。
`main` への直接pushはサーバー側で拒否されます（`GH013: Changes must be made through a pull request.`）。
併せて、`main` の削除と強制pushも禁止しています。レビュー承認は必須にしていません（1人開発のため）。

本体リポジトリはFreeプランのprivateリポジトリでこの機能が使えないため、
手元のGit hookとpush検知ワークフローで代替しています。保護の強さがリポジトリごとに
異なるのはこの制約によるものです。詳細は本体リポジトリの `.ci/README.md` を参照してください。

## バージョン表記のルール

**対象バージョンは `docs/index.html` の `<span class="version">` 1箇所だけに書きます。**
リリース時に更新するのもここだけです。

以前は全14ページの `<title>` に版を書いていましたが、リリースのたびに14箇所を手で更新する必要があり、
実際には変更したページだけ更新した結果、R25〜R27.2までばらばらにドリフトしていました。
更新箇所が1つなら、ずれようがありません。

- サブページの `<title>` に対象バージョンを書かない
- サブページの本文に「R27.3を導入します」のような**現行版を指す記述**を書かない
  （「最新版」と書き、必要なら目次へリンクする）
- 版付きのファイル名（`MirrorBallLightController_R27.3.unitypackage`）を書かない。
  `MirrorBallLightController_*.unitypackage` のように版非依存で書く
- **機能の履歴ラベルは書き換えない。** 「R25 表面Emission」「R25.2 ワールド法線の反映」のような
  「どの版で入った機能か」を示す記述は履歴なので、現行版へ更新するのは誤りです

`.ci/Validate-Docs.ps1` がこれらを検査します（CIでも自動実行されます）。

```
pwsh ./.ci/Validate-Docs.ps1
```

## フォルダ構成のルール

- 現行版マニュアルは **`manual/README.md`**（フォルダ名にバージョンを入れません）
- 版を更新するときは、直前の版の**コピー**を `archive/R<バージョン>_GitHub_Manual/` へ残し、
  `manual/README.md` はその場で書き換える（フォルダ名は変えない）
- `docs/` は現行版のHTMLマニュアルだけを置く
- `R25/README.md` は**削除しないでください**

### なぜフォルダ名にバージョンを入れないか

以前は `R25/` `R26/` `R27.2/` のように現行版フォルダを毎回改名していました。この方式には
2つの問題がありました。

1. 本体リポジトリのREADMEが現行版フォルダを指しているため、**改名のたびに別リポジトリの
   リンクを張り替える必要がある**。順序を誤ると一時的に404になります
2. 改名を忘れると、フォルダ名と中身のバージョンがずれる。実際にR27.3リリース後も
   フォルダ名は `R27.2/` のまま残っていました

`manual/` に固定すれば、本体側のリンクは一度設定すれば以後変更不要です。
「どの版のマニュアルか」は中身と `docs/index.html` の対象バージョン表記で示します。
ブランチ名を `work/YYYYMMDD-NN` にしたのと同じ考え方です。

### R25/README.md を残している理由

R25〜R27.3の配布済みUnityPackageには、同梱READMEに `main/R25/README.md` へのリンクが
焼き込まれています。配布済みパッケージ内のリンクは後から修正できないため、このパスを削除すると
既にダウンロード済みのユーザーがマニュアルを開いたときに恒久的な404になります。

そのため `R25/README.md` は移転案内ページとして残しています。中身は最新版とアーカイブへの誘導だけです。

なお、この問題を繰り返さないよう、本体リポジトリの同梱READMEは版に依存しないURL
（リポジトリのトップページ）だけを案内する形に変更済みです。

## 版を更新するときの手順

1. `work/YYYYMMDD-NN` ブランチを作る
2. 更新前の `manual/README.md` を `archive/R<バージョン>_GitHub_Manual/README.md` へコピーする
3. `manual/README.md` と `docs/` の各ページを新しい内容へ更新する
4. `docs/index.html` の対象バージョン表記を更新する（**バージョンを書くのはここだけ**）
5. `pwsh ./.ci/Validate-Docs.ps1` で表記とリンクを検証する
6. PRを作成してマージする

フォルダ名を固定したため、本体リポジトリ側のリンク張り替えは不要になりました。
ただし本体側の記述内容（対応バージョンの説明など）を変える必要がある場合は、
**マニュアルのマージ後に**本体側を更新してください。
